## 2026-07-25 0.3.10 自适应传输窗口

- 用户确认不做 WebSocket/WSS，选择继续优化分块窗口方案。
- 实际处理：跨浏览器文件发送从固定 4 分块窗口升级为 2—8 分块自适应窗口；确认快时逐步加窗口，确认超过 1.5 秒时自动收敛。
- 验证目标：继续保留 256 KiB 单块、逐块确认和最终完整性校验，避免在移动端过度堆积内存。

## 2026-07-25 0.3.9 跨浏览器传输流水线

- 用户反馈：跨浏览器设备码互传速度偏慢。
- 实际处理：页面扩展发送文件时不再每个 256 KiB 分块完全串行等待，改为最多 4 个分块同时等待确认，提高 SRL 与 SillyTavern 双向传输吞吐。
- 稳定性边界：保持原分块大小、单文件 256 MB 限制、逐块确认和最终 SHA-256 校验；中继服务端仍只保存短时内存队列，不落盘保存资源。
- 版本处理：发布版本提升到 0.3.9，方便 SillyTavern 扩展管理器和用户确认已更新。
- 验证：`node scripts/check.mjs` 通过，0.3.9 release 包重新生成。

# 工作日志

## 2026-07-23 0.3.8 安卓配对修复

- 同浏览器流程移除 `bridge.html + iframe` 中间层，酒馆扩展直接打开用户配置的 SRL 地址并以受控 `postMessage + MessageChannel` 配对。
- 跨浏览器加入不再相信手机浏览器回传的 `target`；服务端使用设备码创建时保存的 `srlUrl` 与 `srlOrigin`，避免同站重定向、尾斜杠和入口差异造成误拒绝。
- 增加无 `target` 加入测试、不同入口加入测试，以及禁止同浏览器回退 iframe 的结构检查。
- 验证：10 项协议与适配器测试及发布结构检查通过。

## 2026-07-23 0.3.7 设备码旧插件防回退

- 将新版设备码加入入口从 `/join` 升级为 `/join-v2`，让旧服务端插件直接暴露版本不兼容，而不是继续运行 iframe 旧页面。
- 页面扩展和服务端插件统一升级到 0.3.7；补充测试确保新路由返回无 iframe 的轻量中继页。

## 2026-07-23 0.3.6 顶层资源库中继

- 设备码加入由“跳转到酒馆并 iframe 嵌入 SRL”改为“原 SRL 保持顶层 + 酒馆中继窗口转交 MessagePort”。
- 中继窗口不再加载或保存资源，只负责同源轮询酒馆服务端；修复跨站 iframe IndexedDB 被浏览器分区后看似数据不互通的问题。
- 加入页显式使用 `Cross-Origin-Opener-Policy: unsafe-none`，配合 SRL 的 `same-origin-allow-popups` 保留受控窗口关系。
- 9 项扩展与服务端插件测试通过。

## 2026-07-23 · 跨平台路径识别安装器

- 修复 README 一行安装命令写死 `D:\SillyTavern\SillyTavern` 的问题，Windows 安装路径改为可选参数并自动检查当前目录、用户常用目录与各磁盘常见位置。
- 多个酒馆副本不再擅自选择，安装器会列出路径让用户确认；自动识别失败才要求粘贴真实路径，并支持全局模式单独指定配置文件。
- 新增 Bash 安装器覆盖 Android/Termux、Linux 和 macOS，优先识别官方常见 `~/SillyTavern`，沿用下载、校验、旧版备份、安装和启用配置流程。
- 完整发布包同步携带 PowerShell 与 Bash 安装脚本，文档区分 Android 本机运行酒馆与 iOS/手机仅远程访问两种情况。

## 2026-07-23 · 安装故障排查与服务端一键安装

- 本机发现 GitHub 版 `SillyTavern-SRL-Bridge` 已成功克隆，同时残留旧手动版 `srl-bridge`；重复安装会命中已存在目录并显示失败。
- README 增加已安装判断、重复副本清理、正确 `.git` 地址和网络故障排查。
- 新增 Windows PowerShell 安装器：校验酒馆根目录、下载最新服务端 Release、备份旧插件、写入插件目录并启用服务端插件配置。
- 真实联网测试发现 Windows PowerShell 默认 TLS 无法连接 GitHub，已显式启用 TLS 1.2 并重新验证下载路径。
- 明确酒馆助手属于页面扩展，不能越权写服务器目录；一行网络安装同时标注供应链风险。

## 2026-07-23 · 互传类型与浏览器识别

- 将互传范围由角色卡、世界书、预设扩展为六类资源，新增全局正则、快速回复组和酒馆主题。
- 正则通过扩展公开设置保存；快速回复通过 `quickReplyApi`；主题通过 `/api/themes/save`。
- 扩展面板识别并显示当前浏览器，避免用户误以为网页能选择其他浏览器的本地数据库。
- 明确跨浏览器必须增加酒馆服务端中继；设备码只作为短时会话索引，不能作为永久密钥。

## 2026-07-23

- 新建独立扩展仓库、清单、设置界面、协议层、酒馆适配器和连接控制器。
- 完成角色卡、世界书、当前 API 类型预设的目录读取、导出与导入；同名资源支持副本、跳过和覆盖。
- 增加六位配对码、精确来源校验、一次性 `MessageChannel`、256 KiB 分块、256 MB 限制和 SHA-256 完整性校验。
- 真实联调发现 SillyTavern 1.18.0 的 COOP 会切断跨来源 `window.opener`，改为扩展同源中继页承载 SRL iframe 后解决。
- 自动化验证通过：5 项单元测试；真实 SillyTavern 与 SRL 配对、读取 10 项资源、拉取 1 项进入 SRL，并验证 390×844 无横向溢出。
- 扩展已复制到当前 `default-user` 的第三方扩展目录；未写入用户密码、令牌或酒馆资源文件。
- 根据用户反馈重做酒馆端设置 UI：接入原生 `inline-drawer` 并默认折叠，标题栏保留连接状态；展开后采用紧凑档案交换站布局。
- 首轮真实截图发现酒馆扩展列在桌面也可能只有约 280px，原按钮被挤成竖排；改用 CSS 容器查询后，标题、状态、标签和操作按钮会按插件实际宽度重排。
- 手机端增加局域网地址说明；从非回环地址打开酒馆时，回环 SRL 配置自动替换为当前酒馆主机的 5173 端口。
- 再次完成真实配对、读取 10 项资源、拉取 1 项、默认折叠/展开和 390×844 无横向溢出验证；生成桌面与手机真实酒馆预览图。

## 2026-07-23 0.3.0

- 新增 `server-plugin` 短时设备码中继，跨浏览器通过 SillyTavern 同源加入页与 SRL iframe 建立临时通道。
- 协议升级到 v2，文件块逐块确认；服务端限制单消息 512 KiB、单端队列 2 MiB，资源不落盘。
- 正则互传拆分为全局、角色卡和预设三个作用域；快速回复组与主题保持支持。
- SillyTavern 1.18.0 实际加载通过，服务端创建设备码、加入与双向消息轮询均验证成功。

## 2026-07-23 0.3.1 GitHub 公开发布

- 页面扩展仓库根目录继续保留 `manifest.json`，满足 SillyTavern“安装扩展”直接克隆 Git URL 的要求；补充主页、自动更新和最低客户端版本元数据。
- README 重写为面向普通用户的功能、安装、模式选择、手机连接、安全和常见边界说明，移除开发机绝对路径。
- 服务端插件仍通过 GitHub Release 独立 ZIP 和完整包发布；页面扩展不能越权写入 `SillyTavern/plugins`，因此不会伪装成一键安装。

## 2026-07-23 0.3.4 配对修复

- 同浏览器连接改为每次创建唯一中继窗口，断开时主动关闭，避免浏览器复用旧页面后提示找不到当前酒馆通道。
- 修复跨浏览器加入被错误绑定到创建设备码的酒馆用户身份：八位码两分钟内可由另一浏览器使用，加入后换为双方独立随机长令牌，并继续核对六位确认码。
- 新增不同浏览器身份加入的回归测试；扩展、服务端和完整安装包重新生成。

## 2026-07-23 0.3.5 Termux 安装修复

- Termux 默认安装由 Release ZIP 改为直接下载 `index.mjs` 与 `relay.js`，避开 GitHub 附件 403；Raw GitHub 失败自动尝试 jsDelivr。
- 离线 ZIP 模式不再把 Info-ZIP 的 Windows 分隔符警告直接当成失败，而是以最终文件结构和内容特征校验为准。
- Windows 发布脚本使用 .NET ZipArchive 显式生成 `/` 分隔的条目，保证 Termux、Linux、macOS 可解压。
## 2026-07-25 旧版发布包清理

- 用户要求：确认 GitHub 上的酒馆扩展是否同步，并清理无用文件，避免 SillyTavern 安装/更新看到旧交付内容。
- 实际处理：扩展仓库 `release/` 只保留当前 `0.3.8` 安装包和 `srl-bridge-server-plugin-latest.zip`，删除 `0.3.0` 至 `0.3.7` 的旧完整包、页面扩展包和服务端插件包。
- 同步处理：删除未跟踪的临时 `pnpm-lock.yaml`；源码、manifest、README、当前 release 包不变。
- 验证：`pnpm check` 通过；结构检查和 10 项互传测试全部通过。
## 2026-07-25 安装器替换策略调整

- 用户确认 Termux 正确安装命令执行成功，同时要求更新服务端插件时不要长期保留旧插件，除非新版本安装失败。
- 实际处理：Termux/Linux/macOS 与 Windows 安装器均改为先下载并校验新插件，再临时移走旧 `plugins/srl-bridge`；新插件复制和 `enableServerPlugins` 配置写入全部成功后，默认删除旧插件临时备份。
- 失败保护：如果下载、复制或配置写入中途失败，安装器会自动删除残缺的新目录，并把旧插件恢复回 `plugins/srl-bridge`。
- 兼容保留：新增 `--keep-backup` / `-KeepBackup` 选项，用户需要手动保留旧版时可显式启用。
- 验证：重新生成 0.3.8 发布包；Shell 脚本语法检查、PowerShell 脚本解析和 `pnpm check` 均通过。

## 2026-07-25 移动端中继诊断优化

- 用户反馈：安卓移动端设备码中继页卡在“正在把连接交给原来的资源库页面”。
- 实际处理：服务端中继页增加 12 秒超时提示，说明原 SRL 页面未接收到中继邀请时应刷新资源库、检查弹窗/跨窗口通信。
- 验证：`node scripts/check.mjs` 通过；0.3.8 release 包重新生成。

## 2026-07-25 02:57:38
- 修复设备码中继发送乱序风险：中继页从 SRL 收到的消息改为串行 HTTP 转发，避免 file-start 与 file-chunk 乱序导致 SRL 等待分块确认。
- 酒馆端导入完成后的 file-result 改为等待发送完成。
- 验证：node scripts/check.mjs 通过，release 包重新生成。

## 2026-07-25 跨浏览器传输流水线

- 用户反馈：跨浏览器设备码互传速度偏慢。
- 实际处理：页面扩展发送文件时不再每个 256 KiB 分块完全串行等待，改为最多 4 个分块同时等待确认，提高 SRL 与 SillyTavern 双向传输吞吐。
- 稳定性边界：保持原分块大小、单文件 256 MB 限制、逐块确认和最终 SHA-256 校验；中继服务端仍只保存短时内存队列，不落盘保存资源。
- 验证：`node scripts/check.mjs` 通过，0.3.8 release 包重新生成。
## 2026-07-25 0.3.11 设备码中继保活

- 用户确认希望中继窗口消失后仍能保持约半小时连接；如果中继窗口保留，则连接应持续可用。
- 实际处理：服务端插件新增 active session lastSeen 保活，poll 与 messages 都会刷新活跃时间；中继页关闭不再调用 close；新版中继页会把 participant token 与 relayBase 交给原 SRL 页面，原页面直接轮询酒馆插件。
- 兼容策略：旧浏览器/旧插件仍保留 MessageChannel 交接作为兜底；新版下载包升级到 0.3.11。
