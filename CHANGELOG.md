# 更新记录

## 0.3.26 - 2026-08-12（已发布）

- 临时预览从自建全屏黑色浮层改为插入当前酒馆 `#chat` 的单条临时消息，自动滚动到可见位置；关闭或下次预览即移除，不写入聊天记录。
- 新增受限的前端了么状态栏预览载荷；角色继续使用酒馆当前 `messageFormatting`，美化和状态栏 CSS 只接受 SRL 已作用域隔离后的内容。

## 0.3.25 - 2026-08-12（已发布）

- 新增 `live-preview-v1` 能力：SRL 可将受限的角色开场白或已作用域隔离的美化 CSS 送到真实酒馆页面做临时预览。
- 预览仅创建内存中的临时节点，调用当前酒馆运行时格式化函数；不会导入资源、写聊天、修改预设/设置或执行脚本。载荷保留 SHA-256 与分块校验，单个预览限制为 2 MiB。

## 0.3.24 - 2026-08-12

- 已连接互传会话改为双方有效轮询或传输时续期、连续 4 小时无活动过期；设备码仍为 2 分钟，控制令牌和资源仍不落盘。
- 此变更同步页面扩展发布元数据、服务端插件与离线打包默认版本，适用于 Termux 本机酒馆和其他使用 SRL Bridge 服务端插件的部署。

## 0.3.23 - 2026-08-12

- 修复同机 APK 连接等待两分钟后，酒馆扩展仍保留“允许本机 APK 连接”按钮的问题。扩展现在持续核对服务端等待列表；请求过期时自动收起按钮并提示回 APK 重新发起，避免再出现“请求缺失或已过期”的误导性失败。

## 0.3.22 - 2026-08-12

- 同一台安卓设备上的 APK 现在可直接发起本机连接请求；用户仅需在酒馆扩展点击一次“允许本机 APK 连接”，不再输入设备码或核对确认码。
- 本机服务端仅接受回环请求，批准密钥由页面扩展临时生成且不交给 APK；APK 不能列出、读取或批准等待请求。批准后仍通过短期会话令牌和受限临时端点传输，保留 256 MB、大小/SHA-256 校验和清理机制。
- 跨设备/跨浏览器的 HTTPS 设备码流程及旧版本机兼容流程不变。
- 已发布 GitHub `v0.3.22` Release，包含完整包、页面扩展包和服务端插件包。

## 0.3.21 - 2026-08-11

- 为同一台安卓设备上的 `http://127.0.0.1:8000` 酒馆增加本机直传会话：设备码仍只负责配对和控制消息，文件字节改由带随机 bearer token 的临时本机端点传输。
- 临时文件只存在酒馆插件的随机临时目录，单文件上限 256 MB；上传、下载和最终导入前均保留大小与 SHA-256 校验，完成、过期或插件退出后删除。
- 本机端点不可用时自动回退既有设备码分块传输；非 `127.0.0.1:8000` 部署不声明该能力，也不会开放任意本机地址。

## 0.3.20 - 2026-08-01

- 发布打包脚本的默认版本同步为 `0.3.20`，避免未显式传入版本时生成与扩展元数据不一致的 0.3.19 ZIP 文件名。
- 新增 `userPersona` 与 `userAvatar` 互传类型。人设 JSON 使用酒馆 `powerUserSettings.personas`、`persona_descriptions` 与 `default_persona` 的原生结构导入。
- 用户头像通过官方 `/api/avatars/upload` 上传；人设导入在头像完成后写入设置并调用 `saveSettingsDebounced()`。
- 用户人设不支持“副本”冲突策略，避免复制 JSON 后与头像文件名不一致；跳过和覆盖仍按明确结果返回。

## 0.3.18

- 新增酒馆助手脚本互传：全局（extension_settings.tavern_helper.script.scripts）、角色卡（data.extensions.tavern_helper.scripts）、预设（extensions.tavern_helper.scripts）三个作用域，兼容旧键 TavernHelper / TavernHelper_scripts。
- 导出保持原始 ScriptTree 结构（脚本/文件夹）；导入时统一换新 id 并整树停用，绝不落地即运行，需在酒馆助手中手动启用。
- 导入合并保留同域其他字段（variables 等），冲突策略沿用副本/跳过/覆盖。
- 新增 HelperScripts 单元测试 6 项。

## 0.3.17 - 2026-07-26

- 移动端设备码改用 SRL 云服务器的 HTTPS 内存中继，不再依赖弹窗、中继页返回、`window.opener` 或同浏览器标签页身份。
- 酒馆页面扩展直接向资源库域名创建短期设备码，原资源库输入设备码后在当前页连接；不会再打开一份空白资源库。
- 保留本机 SillyTavern 服务端中继作为旧部署兼容回退；分块确认、SHA-256 校验与 256 MB 单文件限制保持不变。

## 0.3.12 - 2026-07-25

- 设备码中继页“返回原来的资源库”增加 URL 接管兜底：移动浏览器无法自动切回或 `opener/postMessage` 被切断时，会用当前中继窗口跳回 SRL，并携带短期令牌继续完成配对。
- 保留原来的窗口通信路径；能自动切回的浏览器仍优先走原窗口，不行时 1 秒后自动进入兜底。

## 0.3.11 - 2026-07-25

- 设备码中继新增直接会话交接：中继页把短期 participant token 交给原 SRL 页面后，原页面可直接轮询 SillyTavern 服务端插件，不再依赖临时中继窗口长期存活。
- 中继页关闭或切走时不再主动销毁会话；双方仍在轮询时会话保持，任一方消失后约 30 分钟过期。
- “返回原来的资源库”按钮改为优先聚焦原 SRL 标签页；移动浏览器不允许自动切换时，用户手动回到原标签页即可继续使用。

## 0.3.10 - 2026-07-25

- 跨浏览器设备码互传改为自适应流水线窗口：默认 4 个分块，确认快时最高提升到 8 个，确认慢时降到 2 个，减少等待同时控制手机端内存。

## 0.3.9 - 2026-07-25

- 优化跨浏览器设备码互传速度：文件发送改为最多 4 个分块流水线确认，减少每块等待造成的空转时间。
- 继续保留 256 KiB 分块、逐块确认和最终 SHA-256 校验，兼顾手机端内存与稳定性。

## 0.3.8 - 2026-07-23

- 安卓同浏览器连接不再经过页面扩展中间页和 iframe，改为从酒馆直接打开 SRL 顶层页面，避免移动浏览器丢失 `window.opener`。
- 设备码会话只信任酒馆创建会话时锁定的 SRL 地址；加入端只提交短时设备码，不再因重定向或不同入口误报“SRL 来源与设备码不一致”。

## 0.3.7 - 2026-07-23

- 设备码加入入口升级为 `/join-v2`，避免新版资源库误连旧服务端插件后继续打开 iframe 空白资源库。
- 旧服务端插件会明确连接失败并提示升级，不再静默回退到会产生独立 IndexedDB 的旧流程。

## 0.3.6 - 2026-07-23

- 跨浏览器设备码不再把 SRL 嵌入酒馆来源的 iframe；原 HTTPS 资源库保持顶层页面，中继窗口只负责转交通信端口，避免浏览器存储分区生成一份看似空白的新资源库。
- 中继页改为轻量状态窗口，并明确提示保留窗口和返回原资源库；加入页覆盖酒馆的 COOP 响应头以保留受控 opener 通道。

## 0.3.5 - 2026-07-23

- Termux/Linux/macOS 默认安装不再下载 GitHub Release 附件，直接获取服务端插件的两个源码文件；Raw GitHub 失败时自动尝试 jsDelivr 镜像。
- 离线 `--package` 仍支持 ZIP；`unzip` 仅因 Windows 路径分隔符返回警告码时继续检查实际解压结果，不再提前退出。
- 发布脚本改为显式写入 ZIP 标准 `/` 路径，避免不同 Windows PowerShell/压缩工具产生反斜杠条目。

## 0.3.4 - 2026-07-23

- 修复跨浏览器设备码被酒馆登录身份二次拦截、导致另一浏览器无法加入的问题；设备码仍为两分钟一次性凭证，并继续使用随机会话令牌和六位确认码。
- 同浏览器连接改用每次独立的中继窗口，断开时关闭旧窗口，避免浏览器复用残留页面后出现“没有找到酒馆窗口”。
- 中继页在需要登录 SRL 时显示明确进度，登录完成后继续自动配对。

## 0.3.3 - 2026-07-23

- Windows 安装器取消必填固定路径：自动搜索常见目录，多个安装可交互选择，并兼容全局模式的独立 `config.yaml`。
- 新增 Android/Termux、Linux 与 macOS 安装器，自动识别官方常见的 `~/SillyTavern`，也允许显式指定任意路径。
- 完整发布包内置两种安装脚本；文档明确 iPhone/iPad 只需在真正运行酒馆的电脑或服务器安装。

## 0.3.2 - 2026-07-23

- 增加 Windows 服务端插件一键安装脚本，自动下载最新 Release、备份旧版并启用 `enableServerPlugins`。
- 安装脚本显式使用 TLS 1.2，兼容旧版 Windows PowerShell 访问 GitHub。
- 自动下载改用固定的 latest Release 资源，不依赖匿名 GitHub API 配额。
- Git 安装文档统一使用仓库 `.git` 地址，并增加重复目录、重复扩展和网络失败排查。
- 移除扩展清单中已弃用的 `requires` / `optional` 空字段。

## 0.3.1 - 2026-07-23

- 发布公开 GitHub 安装源，可在 SillyTavern 中直接粘贴仓库地址安装和更新页面扩展。
- 补充扩展主页、功能说明、兼容版本和服务端插件独立安装教程。
- 服务端中继继续通过 Release ZIP 与完整安装包分发，不让页面扩展越权写入服务端目录。

## 0.3.0 - 2026-07-23

- 新增短期设备码服务端中继，支持不同浏览器及移动端配对。
- 正则拆分为全局、角色卡、预设三种作用域，导入时写回正确所属对象。
- 分块传输增加逐块确认，限制内存队列，避免大文件一次性堆积。

## 0.2.0 - 2026-07-23

- 新增全局正则、快速回复组和酒馆主题双向互传。
- 在扩展中显示当前浏览器名称，明确同浏览器直连的数据边界。
- 补充跨浏览器设备码中继的安全与能力说明。

## 0.1.1 - 2026-07-23

- 酒馆设置改为默认折叠的原生扩展抽屉，重做连接状态、地址提示、操作区和窄栏响应式排版。
- 手机从局域网地址打开酒馆时，默认 SRL 地址自动使用相同主机的 5173 端口，不再误用手机自身的 127.0.0.1。

## 0.1.0 - 2026-07-23

- 首次提供 SRL 与 SillyTavern 的角色卡、世界书和预设双向互传。
- 增加一次性配对、同源中继、分块传输、完整性校验和冲突策略。
- 增加桌面与移动端适配的 SRL 资源选择界面。
## 0.3.8 - 2026-07-25

- 清理仓库内旧版 release ZIP，只保留当前 `0.3.8` 和 latest 服务端插件包，减少 SillyTavern 从 GitHub 安装/更新时的仓库体积。
## 0.3.8 - 2026-07-25

- 调整服务端插件安装器：新版本安装成功后默认删除旧插件临时备份；安装失败时自动恢复旧插件；需要保留旧版时可追加 `--keep-backup` / `-KeepBackup`。

## 0.3.8 - 2026-07-25

- 服务端中继页增加移动端卡住时的超时诊断提示，避免长期停留在“正在把连接交给原来的资源库页面”但没有下一步说明。

## 2026-07-25 02:57:38
- 修复设备码中继发送资源到酒馆时可能卡住的问题：中继页现在串行转发消息，避免文件开始与分块消息乱序。

## 2026-07-25

- 优化跨浏览器设备码互传速度：文件发送改为最多 4 个分块流水线确认，减少每块等待造成的空转时间。
- 继续保留 256 KiB 分块、逐块确认和最终 SHA-256 校验，兼顾手机端内存与稳定性。
